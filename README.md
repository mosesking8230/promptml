# 📝 promptml - Safe prompt templates for Rust

[![Download promptml](https://img.shields.io/badge/Download%20promptml-blue-grey?style=for-the-badge)](https://github.com/mosesking8230/promptml/releases)

## 🚀 Getting Started

promptml helps you work with LLM prompt templates in Rust. It checks for missing prompt variables at compile time, so your app can catch mistakes before it runs.

If you want to use it on Windows as an end user, visit the release page, download the latest Windows file, and run it on your PC:

[Visit the downloads page](https://github.com/mosesking8230/promptml/releases)

## 💻 What You Need

To run promptml on Windows, use a computer with:

- Windows 10 or Windows 11
- A modern 64-bit processor
- At least 4 GB of RAM
- 100 MB of free disk space
- Internet access for the first download

If your PC can run recent desktop apps, it should work fine.

## 📥 Download promptml

Go to the release page and look for the latest version:

[Download promptml from GitHub Releases](https://github.com/mosesking8230/promptml/releases)

On the releases page, choose the file that matches Windows. Common file types include:

- `.exe` for a direct app file
- `.zip` for a compressed folder
- `.msi` for a Windows installer

If you see more than one file, pick the one marked for Windows or the one that matches your system.

## 🛠️ Install and Run on Windows

### 1. Download the file

Open the release page and get the latest Windows file.

### 2. Open the file

- If you downloaded an `.exe`, double-click it
- If you downloaded an `.msi`, double-click it and follow the install steps
- If you downloaded a `.zip`, right-click it, choose Extract All, then open the extracted folder

### 3. Allow the app to start

Windows may show a security prompt when you open a new file. If that happens:

- Click More info if you see it
- Click Run anyway if you trust the source
- Or click Yes when Windows asks for permission

### 4. Finish setup

If the app uses an installer, follow the on-screen steps:

- Accept the default install path unless you want a different one
- Keep the suggested settings
- Wait for the setup to finish

### 5. Launch promptml

After setup, open promptml from:

- The desktop shortcut
- The Start menu
- The folder where you extracted the files

## 🧭 First Run

When promptml opens for the first time, you may see a simple prompt template screen. Use it to:

- Load or view prompt templates
- Check prompt variables
- Prepare prompts for LLM tools
- Review template text before use

If the app asks for a folder or file, choose the location where you saved your templates.

## ✨ What promptml Does

promptml is built for prompt templates in Rust. It is useful when you want clean prompt files and fewer mistakes.

It helps with:

- Prompt variable checks
- Safer template use
- Better structure for LLM prompts
- Faster review of prompt text
- Fewer missing fields at build time

This makes it a good fit for people who build tools around chatgpt, llm, prompt engineering, and other prompt-based workflows.

## 📁 Typical Use

A common workflow looks like this:

1. Write a prompt template
2. Add named variables like `name`, `topic`, or `style`
3. Let the app check the template
4. Fill in the values
5. Send the final prompt to your LLM tool

This helps keep prompt work neat and easy to repeat.

## 🔧 Troubleshooting

### The file does not open

Try these steps:

- Download the file again
- Make sure the download finished
- Check that you opened the Windows version
- Right-click the file and choose Run as administrator

### Windows blocks the app

If Windows shows a safety prompt, use the options that let you open the file. This can happen with new releases or files from the internet.

### The app looks empty

Try:

- Closing and opening it again
- Checking if you need to load a template file
- Looking in the release notes for setup steps

### The download is a ZIP file

If you downloaded a ZIP file:

- Right-click it
- Select Extract All
- Open the extracted folder
- Start the app file inside that folder

## 🧩 File Types You May See

The release page may include files like:

- Windows installer files
- Standalone app files
- Source code files
- Archive files

For most users, the best choice is the Windows app or installer file, not the source code.

## 🔒 Why This Tool Helps

Prompt work can break when one variable is missing or spelled the wrong way. promptml helps catch those issues early.

That means:

- Less guesswork
- Fewer broken prompts
- More reliable output
- Cleaner prompt files
- Better control over repeated prompt use

## 🖱️ Quick Download Steps

1. Open the releases page
2. Find the latest version
3. Choose the Windows file
4. Download it
5. Open it or install it
6. Start the app

Use this link to get the latest release:

[Open promptml releases](https://github.com/mosesking8230/promptml/releases)

## 🏷️ Topics Covered

promptml fits work around:

- chatgpt
- llm
- llm prompt engineering
- llm prompting
- llm prompts
- perplexity
- prompt
- prompt engineering
- prompt templates
- Rust-based tools

## 📌 Notes for Windows Users

- Keep the file in a folder you can find again
- Use your Downloads folder if you are not sure where to save it
- If the app does not start, restart Windows and try again
- Keep the release page link in case you need a newer version later

## 🧰 Simple Folder Setup

If you use a ZIP file, this folder layout works well:

- `Downloads`
  - `promptml.zip`
- `Documents`
  - `promptml`
  - `templates`

Store your prompt files in one place so they are easy to find.

## 🪄 Using prompt templates well

When you make prompts, keep each template clear:

- Use short variable names
- Keep one task per template
- Put repeated text in one place
- Check the final prompt before use
- Save versions when you make changes

This keeps your prompt work easy to follow

## 📦 Release Page

The latest Windows download is here:

[https://github.com/mosesking8230/promptml/releases](https://github.com/mosesking8230/promptml/releases)